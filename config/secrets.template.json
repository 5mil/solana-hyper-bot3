{
  "comment": "This is a template file. DO NOT add actual secrets here.",
  "required_environment_variables": {
    "WALLET_KEYPAIR": "Base58 encoded Solana keypair for signing transactions",
    "GITHUB_CONTAINER_REGISTRY_PAT": "GitHub Personal Access Token with packages:write scope for pushing Docker images",
    "EMERGENCY_SWEEP_ADDRESS": "Solana address to sweep funds to in emergency situations"
  },
  "optional_environment_variables": {
    "RPC_URL": "Custom Solana RPC endpoint (defaults to public endpoint)",
    "JUPITER_API_KEY": "Optional API key for Jupiter aggregator",
    "DRIFT_API_KEY": "Optional API key for Drift Protocol",
    "MARGINFI_API_KEY": "Optional API key for MarginFi",
    "ALERT_WEBHOOK_URL": "Webhook URL for alerts and notifications"
  },
  "usage": "Set these environment variables before running the bot. Never commit actual values to version control."
}
